# ==========================================
# DflayerApi - Variables de Entorno
# ==========================================
# Copia este archivo como .env y configura los valores según tu entorno

# ==========================================
# Configuración General del Proyecto
# ==========================================
PROJECT_NAME=DflayerApi
VERSION=0.1.0
API_V1_STR=/api/v1
DEBUG=True

# ==========================================
# Seguridad
# ==========================================
SECRET_KEY=change-this-secret-key-in-production-generate-a-secure-random-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# ==========================================
# Base de Datos
# ==========================================
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/dflayer_db

# PostgreSQL (para Docker)
POSTGRES_USER=dflayer_user
POSTGRES_PASSWORD=dflayer_password
POSTGRES_DB=dflayer_db
POSTGRES_PORT=5432

# ==========================================
# CORS
# ==========================================
# Orígenes permitidos separados por comas
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ==========================================
# URL Base de la API (IMPORTANTE para endpoints de archivos)
# ==========================================
# Esta URL se usa para generar las URLs de los archivos subidos a S3
# Ejemplos:
#   - Producción: https://api.dflayer.com
#   - Desarrollo local: http://localhost:8000
#   - Docker local: http://localhost:8000
#
# Si no se configura, se inferirá automáticamente desde el request
# pero es recomendable configurarla explícitamente en producción
BASE_URL=http://localhost:8000

# ==========================================
# AWS S3 - Configuración de Almacenamiento
# ==========================================
# Credenciales de AWS para acceso a S3
# Requeridas para el funcionamiento de /api/v1/files/upload

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# Región de AWS (ej: us-east-1, us-west-2, eu-west-1)
AWS_REGION=us-east-1

# Nombre del bucket de S3
AWS_S3_BUCKET_NAME=your-s3-bucket-name

# Endpoint URL de S3 (solo si usas S3-compatible como MinIO, DigitalOcean Spaces, etc.)
# Dejar vacío para AWS S3 estándar
# Ejemplos:
#   - MinIO: http://localhost:9000
#   - DigitalOcean Spaces: https://nyc3.digitaloceanspaces.com
AWS_S3_ENDPOINT_URL=

# ==========================================
# Email - Configuración SMTP (Gmail compatible)
# ==========================================
EMAIL_PROVIDER=smtp
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=DflayerApi
SMTP_USE_TLS=True

# ==========================================
# Email - SendGrid (Alternativa a SMTP)
# ==========================================
# Si prefieres usar SendGrid, cambia EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_FROM_EMAIL=noreply@dflayer.com
# SENDGRID_FROM_NAME=DflayerApi

# ==========================================
# SMS - Twilio
# ==========================================
SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_FROM_NUMBER=+1234567890

# ==========================================
# Frontend URL
# ==========================================
# URL del frontend para enlaces en emails
FRONTEND_URL=http://localhost:3000

# ==========================================
# Transcoding (Opcional)
# ==========================================
TRANSCODING_WORKER_COUNT=4
TRANSCODING_MAX_RETRIES=3
TRANSCODING_QUEUE_SIZE=100
FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe

# ==========================================
# Docker - Puerto de la API
# ==========================================
API_PORT=8000

